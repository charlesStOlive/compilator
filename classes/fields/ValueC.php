<?php namespace Waka\Compilator\Classes\fields;

class ValueC extends BaseC
{
    public function __construct($model, $key, $config, $options)
    {
        parent::__construct($model, $key, $config, $options);
    }
}
